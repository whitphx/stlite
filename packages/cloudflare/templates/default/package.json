{
  "name": "__PROJECT_NAME__",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "stlite-cloudflare build",
    "dev": "stlite-cloudflare dev",
    "deploy": "stlite-cloudflare deploy",
    "clean": "stlite-cloudflare clean"
  },
  "devDependencies": {
    "@stlite/cloudflare": "^__PACKAGE_VERSION__",
    "wrangler": "^4.105.0"
  }
}
