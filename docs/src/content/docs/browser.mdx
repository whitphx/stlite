---
title: "@stlite/browser"
description: Mount a Streamlit app on a static web page.
---

import { Code } from '@astrojs/starlight/components';
import pkg from '@stlite/browser/package.json';

`@stlite/browser` allows you to mount a Streamlit app on a static web page using a `<script>` tag.

{/* Import demo files from SSoT location */}
import customElementHtml from '../../../../packages/browser/demos/custom-element/index.html?raw';
import mountHtml from '../../../../packages/browser/demos/basic-mount/index.html?raw';
import multipageHtml from '../../../../packages/browser/demos/multipage/index.html?raw';
import requirementsHtml from '../../../../packages/browser/demos/requirements/index.html?raw';
import sharedWorkerHtml from '../../../../packages/browser/demos/shared-worker/index.html?raw';

{/* Helper to replace placeholders with CDN URLs */}
export const cdnBase = `https://cdn.jsdelivr.net/npm/@stlite/browser@${pkg.version}/build`;
export const toCdn = (html) => html
  .replace(/\{\{STLITE_JS_URL\}\}/g, `${cdnBase}/stlite.js`)
  .replace(/\{\{STLITE_CSS_URL\}\}/g, `${cdnBase}/stlite.css`);

## Installation & Usage

You can use _Stlite_ on your web page by loading the JavaScript and CSS files via `<script>` and `<link>` tags.

### Using `<streamlit-app>` tag

The easiest way is using the `<streamlit-app>` custom element.

<Code code={toCdn(customElementHtml)} lang="html" title="hello-world.html" />

### Using `mount()` function

For more control, you can use the `mount()` function.

<Code code={toCdn(mountHtml)} lang="html" title="mount.html" />

## Advanced Features

### Multipage apps

You can create multipage apps by mounting multiple files, including an entrypoint and page files in a `pages/` directory.

<Code code={toCdn(multipageHtml)} lang="html" title="multipage.html" />

### Installing Packages

Use the `requirements` option to install Python packages.

<Code code={toCdn(requirementsHtml)} lang="html" title="requirements.html" />

### SharedWorker Mode

To reduce memory consumption when running multiple Stlite apps, enable `SharedWorker` mode.

<Code code={toCdn(sharedWorkerHtml)} lang="html" title="shared-worker.html" />

:::note
SharedWorker does not work in some environments like Chrome for Android. Stlite will fall back to standard Workers in these cases.
:::

## API Reference

### `mount(options, container)`

Mounts the Streamlit app to the given HTML container.

**Parameters:**

- `options`: `object` | `string`
  - If a string is provided, it is treated as the content of the `streamlit_app.py` file.
  - If an object is provided, it must be a `MountOptions` object (see below).
- `container`: `HTMLElement` (optional, default: `document.body`)
  - The DOM element to mount the app into.

**Returns:**

A `StliteController` object with the following methods:

- `unmount()`: Unmounts the app and disposes the kernel.
- `install(requirements)`: Installs additional packages.
- `writeFile(path, data, opts)`: Writes a file to the virtual filesystem.
- `readFile(path, opts)`: Reads a file from the virtual filesystem.
- `unlink(path)`: Deletes a file.
- `renameFile(oldPath, newPath)`: Renames a file.

### `MountOptions`

Configuration object for `mount()`.

| Property | Type | Description |
| :--- | :--- | :--- |
| `entrypoint` | `string` | The file path of the main script (e.g., `"streamlit_app.py"`). Required if `files` is an object. |
| `files` | `Record<string, FileContent>` | An object mapping file paths to their content. Content can be a string, `Uint8Array`, or an object `{ url: string }`. |
| `requirements` | `string[]` | A list of Python packages to install at startup. |
| `streamlitConfig` | `Record<string, any>` | Streamlit configuration options (e.g., `{ "client.toolbarMode": "viewer" }`). |
| `archives` | `ArchiveObject[]` | List of archive files (e.g., zip) to download and unpack. |
| `pyodideUrl` | `string` | Custom URL for the Pyodide interpreter. |
| `sharedWorker` | `boolean` | Whether to use a SharedWorker. |
| `idbfsMountpoints` | `string[]` | List of paths to mount with IDBFS (IndexedDB-based persistent file system). |
