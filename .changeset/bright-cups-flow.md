---
"@stlite/desktop": minor
---

Add `appMenu` option to hide the application menu bar. Set `stlite.desktop.appMenu` to `false` in your `package.json` to completely remove the Electron application menu.
