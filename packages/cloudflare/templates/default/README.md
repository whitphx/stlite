# __PROJECT_NAME__

This is a Streamlit app packaged for Cloudflare Python Workers with `@stlite/cloudflare`.

```bash
npm install
npm run dev
npm run deploy
```

Edit `app/streamlit_app.py` to change the app.
