{
  "name": "@stlite/cloudflare",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "./scripts/sync-workers-vendor.sh && npx wrangler deploy",
    "dev": "./scripts/sync-workers-vendor.sh && npx wrangler dev",
    "start": "npm run dev",
    "test": "uv run --project . pytest",
    "smoke:browser": "node scripts/browser-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "wrangler": "^4.105.0"
  }
}
