{
  "name": "stlite-cloudflare-harness",
  "private": true,
  "scripts": {
    "build": "node ../packages/cloudflare/bin/stlite-cloudflare.mjs build",
    "deploy": "node ../packages/cloudflare/bin/stlite-cloudflare.mjs deploy",
    "dev": "node ../packages/cloudflare/bin/stlite-cloudflare.mjs dev",
    "start": "npm run dev",
    "test": "uv run --project . pytest",
    "smoke:browser": "node scripts/browser-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "wrangler": "^4.105.0"
  }
}
